nz = 5
quartz()
image.plot(hf_depth_map[50,,k:1])
plot(1:nx,hf_depth_map[50,,1],type="l",ylim=range(hf_depth_map[50,,]))
for(k in 2:nz){ #was nz (which is 42)
     lines(1:nx,hf_depth_map[50,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)

quartz()
image.plot(hf_depth_map[75,,k:1])
plot(1:nx,hf_depth_map[75,,1],type="l",ylim=range(hf_depth_map[75,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[75,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[100,,k:1])
plot(1:nx,hf_depth_map[100,,1],type="l",ylim=range(hf_depth_map[100,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[100,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)

quartz()
image.plot(hf_depth_map[125,,k:1])
plot(1:nx,hf_depth_map[125,,1],type="l",ylim=range(hf_depth_map[125,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[125,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[150,,k:1])
plot(1:nx,hf_depth_map[150,,1],type="l",ylim=range(hf_depth_map[150,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[150,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[175,,k:1])
plot(1:nx,hf_depth_map[175,,1],type="l",ylim=range(hf_depth_map[175,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[175,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[200,,k:1])
plot(1:nx,hf_depth_map[200,,1],type="l",ylim=range(hf_depth_map[200,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[200,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[225,,k:1])
plot(1:nx,hf_depth_map[225,,1],type="l",ylim=range(hf_depth_map[225,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[225,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[250,,k:1])
plot(1:nx,hf_depth_map[250,,1],type="l",ylim=range(hf_depth_map[250,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[250,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[275,,k:1])
plot(1:nx,hf_depth_map[275,,1],type="l",ylim=range(hf_depth_map[275,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[275,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[300,,k:1])
plot(1:nx,hf_depth_map[300,,1],type="l",ylim=range(hf_depth_map[300,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[300,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[325,,k:1])
plot(1:nx,hf_depth_map[325,,1],type="l",ylim=range(hf_depth_map[325,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[325,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[350,,k:1])
plot(1:nx,hf_depth_map[350,,1],type="l",ylim=range(hf_depth_map[350,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[350,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[375,,k:1])
plot(1:nx,hf_depth_map[375,,1],type="l",ylim=range(hf_depth_map[375,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[375,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[400,,k:1])
plot(1:nx,hf_depth_map[400,,1],type="l",ylim=range(hf_depth_map[400,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[400,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)
quartz()
image.plot(hf_depth_map[425,,k:1])
plot(1:nx,hf_depth_map[425,,1],type="l",ylim=range(hf_depth_map[425,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[425,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)



quartz()
image.plot(hf_depth_map[450,,k:1])
plot(1:nx,hf_depth_map[450,,1],type="l",ylim=range(hf_depth_map[450,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[450,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)

quartz()
image.plot(hf_depth_map[475,,k:1])
plot(1:nx,hf_depth_map[475,,1],type="l",ylim=range(hf_depth_map[475,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[475,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)

quartz()
image.plot(hf_depth_map[500,,k:1])
plot(1:nx,hf_depth_map[500,,1],type="l",ylim=range(hf_depth_map[500,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[500,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)

quartz()
image.plot(hf_depth_map[525,,k:1])
plot(1:nx,hf_depth_map[525,,1],type="l",ylim=range(hf_depth_map[525,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[525,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)

quartz()
image.plot(hf_depth_map[540,,k:1])
plot(1:nx,hf_depth_map[540,,1],type="l",ylim=range(hf_depth_map[540,,]))
for(k in 2:nz){
     lines(1:nx,hf_depth_map[540,,k],col=k)
}
abline(h=pf_depth_list,lty = 3)